---
name: CI

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install toolchain and collections
        run: |
          pip install ansible-core ansible-lint yamllint
          ansible-galaxy collection install kubernetes.core

      - name: Symlink role to its namespace path (required for syntax-check)
        run: |
          mkdir -p ~/.ansible/roles
          ln -s "${GITHUB_WORKSPACE}" ~/.ansible/roles/apellini.data_seed

      - name: yamllint
        run: yamllint -d relaxed .

      - name: ansible-lint
        run: ansible-lint --profile=production

      - name: Syntax check (stub inventory)
        run: |
          cat > /tmp/stub-inventory.yml << 'INV'
          all:
            hosts:
              k3s_stub:
                ansible_connection: local
          INV
          ansible-playbook -i /tmp/stub-inventory.yml --syntax-check tests/test.yml
